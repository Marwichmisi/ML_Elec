---
phase: 01-core-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - Makefile
  - cmd/ml-elec/main.go
  - internal/config/config.go
  - internal/config/config_test.go
  - config.yaml
autonomous: true
requirements:
  - CORE-05
  - CORE-07
must_haves:
  truths:
    - "Config loads from ./config.yaml when present"
    - "Config loads from ~/.config/ml-elec/config.yaml as fallback"
    - "Config returns defaults when no file found"
    - "Disabled plugin in config is not launched"
    - "Core compiles and binary exists at bin/ml-elec"
    - "go.mod contains correct module path and Go version"
  artifacts:
    - path: "go.mod"
      provides: "Module definition with all dependencies"
    - path: "Makefile"
      provides: "Build automation (build, test, wire, lint, check-loc)"
    - path: "internal/config/config.go"
      provides: "Config struct, Load(), DefaultConfig(), Validate()"
    - path: "internal/config/config_test.go"
      provides: "Unit tests for config loading and defaults"
    - path: "config.yaml"
      provides: "Default configuration file"
  key_links:
    - from: "internal/config/config.go"
      to: "config.yaml"
      via: "Load() searches paths and parses YAML"
      pattern: "yaml\\.Unmarshal"
---

<objective>
Set up the Go module, project skeleton, and centralized configuration system.

Purpose: Establishes the project foundation (module, build system) and the first component (config) that all other packages depend on. Without config, no other component can initialize.
Output: Working Go module, Makefile with build/test/lint/check-loc targets, config package with YAML loading from multiple paths, and config.yaml default file.
</objective>

<execution_context>
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/workflows/execute-plan.md
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation/01-CONTEXT.md
@.planning/phases/01-core-foundation/01-RESEARCH.md
@.planning/phases/01-core-foundation/01-PATTERNS.md
@.planning/phases/01-core-foundation/01-SPEC.md

@.agents/skills/golang-error-handling/SKILL.md
@.agents/skills/golang-naming/SKILL.md
@.agents/skills/golang-structs-interfaces/SKILL.md
@.agents/skills/golang-testing/SKILL.md
@.agents/skills/golang-lint/SKILL.md
@.agents/skills/golang-project-layout/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Go module init + Makefile + config package skeleton</name>
  <files>go.mod, go.sum, Makefile, cmd/ml-elec/main.go, internal/config/config.go</files>
  <read_first>
    - .planning/phases/01-core-foundation/01-RESEARCH.md (Standard Stack section for dependency versions)
    - .planning/phases/01-core-foundation/01-PATTERNS.md (config.go pattern, Makefile pattern)
    - .agents/skills/golang-project-layout/SKILL.md (directory conventions)
    - .agents/skills/golang-naming/SKILL.md (package naming)
  </read_first>
  <behavior>
    - Test DefaultConfig() returns non-nil Config with expected field values (NATS port -1, Storage path non-empty, API port 8080)
    - Test Validate() rejects Config with empty NATS host
    - Test Validate() rejects Config with empty Storage path
    - Test Load() returns defaults when no config file exists
  </behavior>
  <action>
Initialize Go module `ml-elec` with `go mod init ml-elec`. Create directory structure per D-01 and D-02:
- `cmd/ml-elec/` (main.go placeholder with package main + blank import of internal/config)
- `internal/config/` (config.go + config_test.go)

Create `Makefile` per D-PATTERNS with targets: `build` (go build -o bin/ml-elec ./cmd/ml-elec), `test` (go test ./...), `test-race` (go test -race ./...), `wire` (wire ./...), `lint` (golangci-lint run), `run` (build + execute), `clean`, `check-loc` (find . -name '*.go' ! -name '*_test.go' ! -path './generated/*' | xargs wc -l, gate at 5000).

Install dependencies per RESEARCH Standard Stack versions:
- `gopkg.in/yaml.v3` for YAML parsing (D-07: net/http standard, no external frameworks)
- `github.com/Masterminds/squirrel` for query building (D-12)
- `modernc.org/sqlite` for pure-Go SQLite driver (D-11)
- `github.com/golang-migrate/migrate/v4` for migrations (D-13)
- `github.com/nats-io/nats-server/v2` for embedded NATS
- `github.com/hashicorp/go-plugin` for plugin lifecycle
- `github.com/google/wire` for compile-time DI (D-05)
- `github.com/rs/cors` for CORS middleware

Implement `internal/config/config.go` per D-PATTERNS config pattern:
- Config struct with NATSConfig, StorageConfig, APIConfig, PluginsConfig sub-structs (all with yaml tags per D-03)
- DefaultConfig() returning sensible defaults: NATS host 127.0.0.1 port -1, Storage path ./data/sensors.db, API port 8080, Plugins enabled empty slice
- Load() function searching: ./config.yaml, then $HOME/.config/ml-elec/config.yaml, then defaults (per D-03)
- loadFromFile(path) reading YAML and unmarshaling into DefaultConfig() overlay
- Validate() checking required fields (NATS host non-empty, Storage path non-empty)
- All errors wrapped with fmt.Errorf("context: %w", err) per Go error handling skill

Create `config.yaml` with documented defaults (NATS host/port, storage path, API port, plugins list).

Create placeholder `cmd/ml-elec/main.go` with package main, blank import `_ "ml-elec/internal/config"`, and a minimal main that will be expanded in Plan 05.
  </action>
  <verify>
    <automated>cd /home/marwane/Documents/My-ML_Elec && go build ./cmd/ml-elec && go test ./internal/config/... -v</automated>
  </verify>
  <acceptance_criteria>
    - go.mod exists with module path "ml-elec" and Go 1.26+
    - Makefile has all 7 targets (build, test, test-race, wire, lint, run, clean, check-loc)
    - internal/config/config.go compiles and exports: Config struct, Load(), DefaultConfig(), Validate()
    - DefaultConfig() returns NATS port -1, API port 8080, Storage path "./data/sensors.db"
    - Validate() returns error for empty NATS host or empty Storage path
    - cmd/ml-elec/main.go exists with package main
    - config.yaml exists with documented YAML config
    - `go test ./internal/config/...` passes
    - `go vet ./...` passes with no warnings
  </acceptance_criteria>
  <done>Go module initialized, Makefile operational, config package loads YAML from multiple paths with fallback to defaults, validates required fields, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Config tests + edge case coverage</name>
  <files>internal/config/config_test.go</files>
  <read_first>
    - internal/config/config.go (the implementation to test)
    - .planning/phases/01-core-foundation/01-SPEC.md (Edge Coverage: missing-config)
    - .agents/skills/golang-testing/SKILL.md (table-driven tests, test patterns)
  </read_first>
  <action>
Write comprehensive unit tests in `internal/config/config_test.go` covering:

1. **DefaultConfig tests**: Verify all fields have expected default values
2. **Load from file tests**: Create temp YAML file, verify Load() reads it correctly
3. **Missing config test (edge: missing-config)**: Delete/move config file, verify Load() returns defaults without error
4. **Invalid YAML test**: Write malformed YAML, verify Load() returns error with context
5. **Validate tests**: Table-driven tests for empty NATS host, empty Storage path
6. **Plugin enable/disable test**: Config with plugins.enabled list, verify structure is correct
7. **Config path search test**: Verify search order (./config.yaml → ~/.config/ml-elec/config.yaml → defaults)

Use `testing.T` and `t.TempDir()` for test isolation. Follow table-driven test pattern per Go testing skill.
  </action>
  <verify>
    <automated>cd /home/marwane/Documents/My-ML_Elec && go test ./internal/config/... -v -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestDefaultConfig: all fields match expected defaults
    - TestLoadFromFile: valid YAML parsed correctly
    - TestLoadMissingFile: returns defaults, no error (covers edge: missing-config)
    - TestLoadInvalidYAML: returns error with context
    - TestValidate: table-driven, covers empty host, empty path
    - TestPluginConfig: enabled list correctly parsed
    - All tests pass with `-count=1` (no cache)
    - `go vet ./internal/config/...` clean
  </acceptance_criteria>
  <done>Config package fully tested: loads from file, falls back to defaults on missing file, validates required fields, handles invalid YAML. Edge case missing-config covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File system → Config | Config file read from disk — untrusted path, could be tampered |
| Environment → Config | Environment variables could override config values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | config.yaml | low | accept | Config file is local, not user-facing in v1; no secrets stored (per prohibition) |
| T-01-02 | Information Disclosure | config loading | low | accept | No secrets in config (prohibition verified in tests) |
</threat_model>

<verification>
- `go build ./cmd/ml-elec` succeeds (binary created)
- `go test ./internal/config/... -v` all tests pass
- `go vet ./...` clean
- `make check-loc` passes (under 5000 LOC)
- Config loads from file when present, defaults when absent
- Validate() rejects invalid configs
</verification>

<success_criteria>
- Go module initialized with all dependencies
- Makefile with all build/test/lint/check targets
- Config package: Load(), DefaultConfig(), Validate() implemented and tested
- config.yaml with documented defaults
- All tests pass, go vet clean
</success_criteria>

<output>
Create `.planning/phases/01-core-foundation/01-01-SUMMARY.md` when done
</output>
