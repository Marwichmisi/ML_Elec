---
phase: 01-core-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - internal/api/server.go
  - internal/api/health.go
  - internal/api/sensors.go
  - internal/api/api_test.go
autonomous: true
requirements:
  - CORE-03
must_haves:
  truths:
    - "GET /health returns 200 OK with JSON body {\"status\": \"ok\"}"
    - "GET /api/v1/sensors?sensor_id=X returns sensor readings as JSON"
    - "GET /api/v1/sensors without sensor_id returns 400 Bad Request"
    - "Concurrent requests are handled without data race"
    - "API server starts on configurable port"
    - "JSON response structure matches {\"data\": ..., \"error\": ...} format"
  artifacts:
    - path: "internal/api/server.go"
      provides: "HTTP server setup, route registration, CORS middleware"
    - path: "internal/api/health.go"
      provides: "GET /health endpoint handler"
    - path: "internal/api/sensors.go"
      provides: "GET /api/v1/sensors endpoint handler"
    - path: "internal/api/api_test.go"
      provides: "Integration tests for API endpoints"
  key_links:
    - from: "internal/api/sensors.go"
      to: "internal/storage/storage.go"
      via: "GetSensorsHandler calls store.GetSensors()"
      pattern: "store\\.GetSensors"
    - from: "internal/api/server.go"
      to: "internal/api/health.go, internal/api/sensors.go"
      via: "mux.HandleFunc registers route handlers"
      pattern: "HandleFunc"
---

<objective>
Build the REST API layer with health check and sensor data query endpoints.

Purpose: Provides the external interface for dashboard and configuration. The API is the primary way external components interact with the core.
Output: net/http server with /health and /api/v1/sensors endpoints, CORS support, OpenAPI annotations, JSON response format per D-10.
</objective>

<execution_context>
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/workflows/execute-plan.md
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-core-foundation/01-CONTEXT.md
@.planning/phases/01-core-foundation/01-RESEARCH.md
@.planning/phases/01-core-foundation/01-PATTERNS.md
@.planning/phases/01-core-foundation/01-SPEC.md
@.planning/phases/01-core-foundation/01-02-SUMMARY.md

@.agents/skills/golang-error-handling/SKILL.md
@.agents/skills/golang-naming/SKILL.md
@.agents/skills/golang-structs-interfaces/SKILL.md
@.agents/skills/golang-testing/SKILL.md
@.agents/skills/golang-security/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API server with health and sensor endpoints</name>
  <files>internal/api/server.go, internal/api/health.go, internal/api/sensors.go</files>
  <read_first>
    - .planning/phases/01-core-foundation/01-RESEARCH.md (Pattern: net/http standard, CORS middleware, response formatting)
    - .planning/phases/01-core-foundation/01-PATTERNS.md (api/server.go, health.go, sensors.go patterns)
    - .planning/phases/01-core-foundation/01-SPEC.md (CORE-03 acceptance criteria, concurrency edge)
    - internal/storage/storage.go (Store interface and SensorReading type)
    - .agents/skills/golang-structs-interfaces/SKILL.md (interface design)
  </read_first>
  <behavior>
    - Test NewServer creates server with correct config
    - Test GET /health returns 200 with {"status": "ok"}
    - Test GET /api/v1/sensors with sensor_id returns 200 with data array
    - Test GET /api/v1/sensors without sensor_id returns 400 with error
    - Test GET /api/v1/sensors with limit parameter respects limit
    - Test concurrent requests (10 goroutines) don't race
  </behavior>
  <action>
Implement `internal/api/server.go` per D-PATTERNS api pattern:
- Server struct holding *http.Server and *storage.Store
- NewServer(cfg *config.APIConfig, store *storage.Store) *Server: Creates http.NewServeMux(), registers routes with method-based patterns ("GET /health", "GET /api/v1/sensors"), configures http.Server with Addr (fmt.Sprintf(":%d", cfg.Port)), ReadTimeout 15s, WriteTimeout 15s, IdleTimeout 60s. Applies CORS middleware via rs/cors per D-PATTERNS.
- Start() error: Calls server.ListenAndServe()
- Shutdown(ctx context.Context) error: Calls server.Shutdown(ctx)

Implement `internal/api/health.go`:
- HealthResponse struct: Status string `json:"status"`
- HealthHandler(w, r): Returns 200 with {"status": "ok"} per D-10 format
- Add swaggo annotation comments per D-09: @Summary Health check, @Tags health, @Produce json, @Success 200, @Router /health [get]

Implement `internal/api/sensors.go`:
- ErrorResponse struct: Error string `json:"error"` per D-10
- writeJSON helper: Sets Content-Type, writes status, encodes JSON
- writeError helper: Uses writeJSON with ErrorResponse
- GetSensorsHandler(w, r): Extracts sensor_id query param (required → 400 if missing), limit param (default 100), calls store.GetSensors(), returns {"data": readings} per D-10
- Add swaggo annotations per D-09

CORS configuration: AllowedOrigins ["*"], AllowedMethods ["GET", "POST", "PUT", "DELETE"], AllowedHeaders ["Content-Type", "Authorization"] — for future dashboard access.
  </action>
  <verify>
    <automated>cd /home/marwane/Documents/My-ML_Elec && go build ./internal/api/... && go vet ./internal/api/...</automated>
  </verify>
  <acceptance_criteria>
    - server.go compiles and exports: Server, NewServer(), Start(), Shutdown()
    - health.go: GET /health returns 200 with {"status": "ok"}
    - sensors.go: GET /api/v1/sensors requires sensor_id, returns {"data": ...} or {"error": ...}
    - JSON response format matches D-10: {"data": ..., "error": ...}
    - CORS middleware applied (headers present in response)
    - Server configures timeouts (read 15s, write 15s, idle 60s)
    - swaggo annotations present on handlers
    - `go build ./internal/api/...` succeeds
    - `go vet ./internal/api/...` clean
  </acceptance_criteria>
  <done>REST API server with /health and /api/v1/sensors endpoints, CORS, JSON response format, OpenAPI annotations. Compiles and vets clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API integration tests with race detection</name>
  <files>internal/api/api_test.go</files>
  <read_first>
    - internal/api/server.go (server implementation to test)
    - internal/api/health.go (health handler)
    - internal/api/sensors.go (sensor handler)
    - internal/storage/storage.go (Store type for mock/test setup)
    - .planning/phases/01-core-foundation/01-SPEC.md (concurrency edge, CORE-03 acceptance)
    - .agents/skills/golang-testing/SKILL.md (httptest, table-driven tests)
  </read_first>
  <action>
Write integration tests in `internal/api/api_test.go`:

1. **TestHealthEndpoint**: Use httptest.NewServer, GET /health, verify 200 + JSON body
2. **TestGetSensorsNoSensorID**: GET /api/v1/sensors (no query param), verify 400 + error JSON
3. **TestGetSensorsValidRequest**: Create temp SQLite DB, insert test data, GET /api/v1/sensors?sensor_id=test&limit=10, verify 200 + data array
4. **TestGetSensorsEmptyResult**: GET with non-existent sensor_id, verify 200 + empty array
5. **TestConcurrentRequests (edge: concurrency)**: Launch 10 goroutines hitting GET /health simultaneously, verify all return 200, run with `-race` flag
6. **TestJSONResponseFormat**: Verify response body has {"data": ...} or {"error": ...} structure

Use `net/http/httptest` for test server. Create temporary SQLite DB for storage tests. Follow table-driven test pattern.
  </action>
  <verify>
    <automated>cd /home/marwane/Documents/My-ML_Elec && go test ./internal/api/... -v -race -count=1</automated>
  </verify>
  <acceptance_criteria>
    - TestHealthEndpoint: 200 OK, body contains "status":"ok"
    - TestGetSensorsNoSensorID: 400 Bad Request, body contains error
    - TestGetSensorsValidRequest: 200 OK, body contains data array
    - TestGetSensorsEmptyResult: 200 OK, empty data array
    - TestConcurrentRequests: 10/10 succeed, no race detected
    - TestJSONResponseFormat: correct JSON structure
    - `go test ./internal/api/... -race` passes (no races)
    - `go vet ./internal/api/...` clean
  </acceptance_criteria>
  <done>API endpoints fully tested: health check, sensor query, parameter validation, concurrent access. Race detector clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → API | External requests cross into core — input validation required |
| API → Storage | API reads/writes to SQLite — parameterized queries prevent injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | GET /api/v1/sensors | medium | mitigate | Parameterized queries via squirrel (D-12), sensor_id validated |
| T-03-02 | Denial of Service | API server | medium | mitigate | ReadTimeout/WriteTimeout/IdleTimeout configured |
| T-03-03 | Information Disclosure | API responses | low | accept | No sensitive data in responses, health check returns minimal info |
</threat_model>

<verification>
- `go test ./internal/api/... -v -race` all tests pass, no races
- `go vet ./...` clean
- GET /health returns 200 with correct JSON
- GET /api/v1/sensors validates input, returns correct responses
- Concurrent requests handled safely
</verification>

<success_criteria>
- API server with /health and /api/v1/sensors endpoints
- JSON response format {"data": ..., "error": ...} per D-10
- CORS middleware for future dashboard
- OpenAPI annotations via swaggo per D-09
- All tests pass with race detector
</success_criteria>

<output>
Create `.planning/phases/01-core-foundation/01-03-SUMMARY.md` when done
</output>
