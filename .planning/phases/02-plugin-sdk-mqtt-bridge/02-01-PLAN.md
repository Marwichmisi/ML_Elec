---
phase: 02-plugin-sdk-mqtt-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pkg/sdk/v1/proto/lifecycle.proto
  - pkg/sdk/v1/proto/sensor.proto
  - pkg/sdk/v1/lifecycle.pb.go
  - pkg/sdk/v1/lifecycle_grpc.pb.go
  - pkg/sdk/v1/sensor.pb.go
  - pkg/sdk/v1/sensor_grpc.pb.go
  - pkg/sdk/v1/plugin.go
  - pkg/sdk/v1/plugin_test.go
  - Makefile
autonomous: true
requirements: [CORE-06]
user_setup:
  - service: protoc
    why: "Protocol buffer compiler required for proto code generation"
    env_vars:
      - name: PROTOC
        source: "System package: sudo apt install protobuf-compiler"
    dashboard_config:
      - task: "Install protoc-gen-go and protoc-gen-go-grpc"
        location: "go install google.golang.org/protobuf/cmd/protoc-gen-go@latest && go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest"

must_haves:
  truths:
    - "Proto files compile without error via make proto"
    - "Generated Go code implements PluginLifecycle and SensorCollector gRPC interfaces"
    - "GRPCPlugin wrapper integrates with go-plugin for plugin lifecycle management"
    - "Existing Phase 1 tests still pass (no regressions)"
  artifacts:
    - pkg/sdk/v1/proto/lifecycle.proto
    - pkg/sdk/v1/proto/sensor.proto
    - pkg/sdk/v1/lifecycle.pb.go
    - pkg/sdk/v1/lifecycle_grpc.pb.go
    - pkg/sdk/v1/sensor.pb.go
    - pkg/sdk/v1/sensor_grpc.pb.go
    - pkg/sdk/v1/plugin.go
    - pkg/sdk/v1/plugin_test.go
  key_links:
    - "proto files → protoc → generated .pb.go files"
    - "plugin.go GRPCPlugin → go-plugin integration"
    - "Makefile proto target → code generation workflow"
---

<objective>
Create the versioned gRPC plugin SDK (CORE-06): proto files for PluginLifecycle and SensorCollector services, generated Go code, Go interface wrappers with go-plugin GRPCPlugin integration, and a Makefile proto target.

Purpose: Establishes the plugin contract that all future plugins (MQTT, Modbus, OPC UA) will implement. The SDK is the foundation of the plugin ecosystem.

Output: Compilable proto files, generated Go code committed in repo, Go interfaces for plugin lifecycle and sensor collection, go-plugin GRPCPlugin wrapper.
</objective>

<execution_context>
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/workflows/execute-plan.md
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-CONTEXT.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-RESEARCH.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-PATTERNS.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-SPEC.md
@internal/plugin/manager.go
@cmd/mock-plugin/main.go
@Makefile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create proto files and Makefile proto target</name>
  <files>pkg/sdk/v1/proto/lifecycle.proto, pkg/sdk/v1/proto/sensor.proto, Makefile</files>
  <read_first>
    - .agents/skills/golang-how-to/SKILL.md
    - .agents/skills/golang-grpc/SKILL.md
    - .agents/skills/golang-naming/SKILL.md
  </read_first>
  <action>
1. Create directory `pkg/sdk/v1/proto/`.

2. Create `pkg/sdk/v1/proto/lifecycle.proto` (per D-05: one proto per service):
   - syntax = "proto3"; package sdk.v1; option go_package = "ml-elec/pkg/sdk/v1";
   - service PluginLifecycle with RPCs: Init(InitRequest) returns (InitResponse), Start(StartRequest) returns (StartResponse), Stop(StopRequest) returns (StopResponse)
   - InitRequest has map&lt;string, string&gt; config = 1
   - All response messages are empty (extensible later)
   - Per D-09: this is the v1 contract, versioned in pkg/sdk/v1/

3. Create `pkg/sdk/v1/proto/sensor.proto`:
   - Same package and go_package
   - service SensorCollector with RPC: Collect(CollectRequest) returns (CollectResponse)
   - CollectRequest: sensor_id (string), topic (string), payload (bytes), payload_format (string — "json" or "binary")
   - CollectResponse: accepted (bool), rejection_reason (string)

4. Add `proto` target to Makefile (per D-06: generated code committed):
   ```
   .PHONY: proto
   proto:
   	protoc --go_out=. --go_opt=paths=source_relative \
   	       --go-grpc_out=. --go-grpc_opt=paths=source_relative \
   	       pkg/sdk/v1/proto/lifecycle.proto \
   	       pkg/sdk/v1/proto/sensor.proto
   ```
   Also update the `build` target to include `go build -o bin/mqtt-plugin ./cmd/mqtt-plugin` (will be needed in Plan 3).

5. Install protoc tools if not present:
   - `go install google.golang.org/protobuf/cmd/protoc-gen-go@latest`
   - `go install google.golang.org/grpc/cmd/protoc-gen-go-grpc@latest`
   - If protoc is not installed, document it as a prerequisite (sudo apt install protobuf-compiler)

6. Run `make proto` to generate .pb.go files.

7. Verify: `ls pkg/sdk/v1/*.pb.go` shows 4 files (lifecycle.pb.go, lifecycle_grpc.pb.go, sensor.pb.go, sensor_grpc.pb.go).
  </action>
  <verify>
    <automated>make proto && ls pkg/sdk/v1/lifecycle.pb.go pkg/sdk/v1/lifecycle_grpc.pb.go pkg/sdk/v1/sensor.pb.go pkg/sdk/v1/sensor_grpc.pb.go</automated>
  </verify>
  <done>
    - Two .proto files exist with correct syntax and go_package
    - make proto generates 4 .pb.go files without error
    - Makefile has proto target and updated build target
    - Generated code compiles: go build ./pkg/sdk/v1/...
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Go SDK interfaces and go-plugin GRPCPlugin wrapper</name>
  <files>pkg/sdk/v1/plugin.go, pkg/sdk/v1/plugin_test.go</files>
  <read_first>
    - .agents/skills/golang-how-to/SKILL.md
    - .agents/skills/golang-error-handling/SKILL.md
    - .agents/skills/golang-naming/SKILL.md
    - .agents/skills/golang-structs-interfaces/SKILL.md
    - .agents/skills/golang-testing/SKILL.md
    - .agents/skills/golang-grpc/SKILL.md
    - internal/plugin/manager.go
  </read_first>
  <behavior>
    - Test: GRPCPlugin implements plugin.GRPCPlugin interface (compile-time check)
    - Test: GRPCServer registers both PluginLifecycle and SensorCollector services
    - Test: GRPCClient returns an object that satisfies PluginLifecycle and SensorCollector interfaces
    - Test: Init/Start/Stop lifecycle calls round-trip through gRPC (in-memory server)
    - Test: Collect call returns accepted=true for valid request
  </behavior>
  <action>
1. Create `pkg/sdk/v1/plugin.go` with Go interfaces and go-plugin integration:

   a. Define interfaces (per D-05, D-07, D-09):
   - `PluginLifecycle` interface: Init(ctx, map[string]string) error, Start(ctx) error, Stop(ctx) error
   - `SensorCollector` interface: Collect(ctx, *pb.CollectRequest) (*pb.CollectResponse, error)

   b. Implement GRPCPlugin struct (per RESEARCH Pattern 1):
   - Embed `plugin.NetRPCUnsupportedPlugin` (gRPC only, no net/rpc)
   - Fields: Impl PluginLifecycle, CollectImpl SensorCollector
   - GRPCServer method: register both services on grpc.Server
   - GRPCClient method: return GRPCClient struct wrapping both gRPC clients

   c. Implement GRPCServer struct (server-side adapter):
   - Embed `pb.UnimplementedPluginLifecycleServer` and `pb.UnimplementedSensorCollectorServer`
   - Delegate Init/Start/Stop to Impl
   - Delegate Collect to CollectImpl

   d. Implement GRPCClient struct (client-side adapter):
   - Fields: lifecycle pb.PluginLifecycleClient, collector pb.SensorCollectorClient
   - Delegate all methods to gRPC clients, wrap errors with fmt.Errorf and %w (per golang-error-handling skill)

2. Create `pkg/sdk/v1/plugin_test.go` with tests:
   - Use `bufconn` for in-memory gRPC testing (per golang-grpc skill)
   - Test lifecycle: create server, register GRPCPlugin, create client, call Init/Start/Stop
   - Test Collect: call Collect with valid request, verify accepted=true
   - Test error propagation: Init returns error, verify client receives it
   - Table-driven tests per golang-testing skill

3. Verify all tests pass with race detector.
  </action>
  <verify>
    <automated>go test -race ./pkg/sdk/v1/... -v</automated>
  </verify>
  <done>
    - PluginLifecycle and SensorCollector interfaces defined
    - GRPCPlugin implements plugin.GRPCPlugin (compile-time verified)
    - GRPCServer and GRPCClient correctly wrap gRPC services
    - All tests pass with race detector
    - go vet clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin → Core | Plugin binary communicates with core via gRPC over go-plugin |
| Proto contract | Defines the API surface — must not expose filesystem or OS access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | Proto contract | medium | mitigate | Proto messages have no filesystem fields; architectural review confirms no fs APIs |
| T-02-02 | Information Disclosure | gRPC transport | low | accept | go-plugin uses in-process pipe, not network — no exposure |
| T-02-SC | Tampering | protoc code generation | high | mitigate | Generated code committed (D-06); verify checksums if supply-chain concern arises |
</threat_model>

<verification>
- `make proto` succeeds and generates 4 .pb.go files
- `go build ./pkg/sdk/v1/...` compiles without error
- `go test -race ./pkg/sdk/v1/...` passes all tests
- `go vet ./pkg/sdk/v1/...` clean
- Existing Phase 1 tests still pass: `go test ./internal/... ./cmd/ml-elec/...`
</verification>

<success_criteria>
- Proto files define PluginLifecycle (Init/Start/Stop) and SensorCollector (Collect) services
- Generated Go code is committed in repo (per D-06)
- Go interfaces provide type-safe plugin contracts
- go-plugin GRPCPlugin wrapper enables gRPC transport
- All tests pass, no regressions
</success_criteria>

<output>
Create `.planning/phases/02-plugin-sdk-mqtt-bridge/02-01-SUMMARY.md` when done
</output>
