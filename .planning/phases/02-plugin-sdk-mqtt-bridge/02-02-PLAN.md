---
phase: 02-plugin-sdk-mqtt-bridge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/config/config.go
  - internal/config/config_test.go
  - internal/storage/migrations/002_assets.up.sql
  - internal/storage/migrations/002_assets.down.sql
  - internal/storage/storage.go
  - config.yaml
autonomous: true
requirements: [ACQ-03, ACQ-04]
user_setup: []

must_haves:
  truths:
    - "Config loads MQTT, validation, and assets sections from YAML with correct defaults"
    - "Asset and asset_sensors tables are created by migration 002"
    - "CreateAsset/GetAsset/ListAssets/CreateAssetSensor methods work correctly"
    - "Config backward compatibility: missing mqtt/validation/assets sections use defaults (per D-26)"
  artifacts:
    - internal/config/config.go (extended)
    - internal/config/config_test.go
    - internal/storage/migrations/002_assets.up.sql
    - internal/storage/migrations/002_assets.down.sql
    - internal/storage/storage.go (extended with asset CRUD)
    - config.yaml (extended)
  key_links:
    - "Config MQTTConfig → MQTT plugin will consume these settings"
    - "Storage migrations → SQLite schema for assets"
    - "Asset CRUD methods → REST API will use them"
---

<objective>
Extend the config system with MQTT, validation, and assets sections (per D-20, D-21, D-26, D-27), and add SQLite migrations + CRUD methods for the asset registry (ACQ-04).

Purpose: Provides the configuration foundation for the MQTT plugin and the data layer for the asset registry. These are prerequisites for Plan 3 (MQTT plugin) and Plan 4 (REST API).

Output: Extended config with sensible defaults, SQLite schema for assets/asset_sensors, and Go CRUD methods for asset management.
</objective>

<execution_context>
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/workflows/execute-plan.md
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-CONTEXT.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-RESEARCH.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-PATTERNS.md
@internal/config/config.go
@internal/storage/storage.go
@internal/storage/migrations/001_init.up.sql
@config.yaml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend config with MQTT, validation, and assets sections</name>
  <files>internal/config/config.go, internal/config/config_test.go, config.yaml</files>
  <read_first>
    - .agents/skills/golang-how-to/SKILL.md
    - .agents/skills/golang-error-handling/SKILL.md
    - .agents/skills/golang-naming/SKILL.md
    - .agents/skills/golang-structs-interfaces/SKILL.md
    - .agents/skills/golang-testing/SKILL.md
    - .agents/skills/golang-safety/SKILL.md
    - internal/config/config.go
  </read_first>
  <behavior>
    - Test: DefaultConfig returns MQTT port 1883, client_id "mqtt-plugin", clean_session false, keep_alive 30
    - Test: DefaultConfig returns validation ranges for temperature (-40..150), humidity (0..100), current (0..1000)
    - Test: DefaultConfig returns validation timestamp drift 5s future, 24h past
    - Test: DefaultConfig returns assets auto_register=true, default_site="factory-1"
    - Test: Load from YAML with all sections works
    - Test: Load from YAML with missing mqtt/validation/assets sections falls back to defaults (per D-26)
    - Test: Load from YAML with partial mqtt section merges with defaults
  </behavior>
  <action>
1. Add new config structs to `internal/config/config.go` (per D-20, D-21):

   a. Add `MQTTConfig` struct:
   - Port int (default 1883, per D-02)
   - ClientID string (default "mqtt-plugin")
   - CleanSession bool (default false, per D-18)
   - KeepAlive int (default 30 seconds)
   - Topics struct: Subscribe string (default "esp32/#"), Status string (default "sys/mqtt-plugin/status")

   b. Add `ValidationConfig` struct:
   - Ranges map[string]RangeConfig (default: temperature -40..150, humidity 0..100, current 0..1000)
   - Timestamp TimestampConfig: MaxFutureDrift string (default "5s"), MaxPastDrift string (default "24h")
   - Health HealthConfig: MaxPayloadSize int (default 1024), MinPayloadSize int (default 1)

   c. Add helper types: RangeConfig{Min,Max float64}, TimestampConfig, HealthConfig

   d. Add `AssetsConfig` struct:
   - AutoRegister bool (default true)
   - DefaultSite string (default "factory-1")

   e. Add fields to Config struct: MQTT MQTTConfig, Validation ValidationConfig, Assets AssetsConfig

   f. Update DefaultConfig() to include defaults for all new sections

   g. Per D-27: Add backward compatibility — if mqtt/validation/assets sections are missing in YAML, use defaults. The existing yaml.Unmarshal behavior already handles this since DefaultConfig() is called first.

2. Create `internal/config/config_test.go`:
   - Table-driven tests for DefaultConfig values
   - Test YAML parsing with all sections
   - Test YAML parsing with missing sections (backward compat per D-26)
   - Test YAML parsing with partial sections (merge behavior)
   - Use t.TempDir() for test config files

3. Update `config.yaml` with new sections:
   - Add mqtt, validation, assets sections with documented defaults
   - Keep existing nats, storage, api, plugins sections unchanged
  </action>
  <verify>
    <automated>go test -race ./internal/config/... -v</automated>
  </verify>
  <done>
    - Config struct has MQTT, Validation, Assets fields
    - DefaultConfig returns correct defaults for all new sections
    - Tests verify default values, YAML parsing, and backward compatibility
    - config.yaml has all new sections documented
    - go vet clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create asset registry migrations and storage CRUD methods</name>
  <files>internal/storage/migrations/002_assets.up.sql, internal/storage/migrations/002_assets.down.sql, internal/storage/storage.go</files>
  <read_first>
    - .agents/skills/golang-how-to/SKILL.md
    - .agents/skills/golang-error-handling/SKILL.md
    - .agents/skills/golang-naming/SKILL.md
    - .agents/skills/golang-database/SKILL.md
    - .agents/skills/sqlite-database-expert/SKILL.md
    - .agents/skills/golang-testing/SKILL.md
    - internal/storage/storage.go
    - internal/storage/migrations/001_init.up.sql
  </read_first>
  <behavior>
    - Test: Migration 002 creates assets table with columns id, name, site, area, line, type, created_at, updated_at
    - Test: Migration 002 creates asset_sensors table with columns id, asset_id, sensor_id, sensor_type, topic, created_at
    - Test: UNIQUE constraint on assets.name prevents duplicates (returns error)
    - Test: UNIQUE constraint on (asset_id, sensor_id) in asset_sensors prevents duplicates
    - Test: CASCADE delete: deleting an asset removes its sensors
    - Test: CreateAsset inserts and returns asset with auto-generated ID and timestamps
    - Test: ListAssets returns paginated results with correct total count
    - Test: GetAsset returns asset by ID or error if not found
    - Test: CreateAssetSensor inserts sensor linked to asset
    - Test: GetAssetSensors returns sensors for a given asset
  </behavior>
  <action>
1. Create `internal/storage/migrations/002_assets.up.sql` (per PATTERNS.md migration pattern):
   - assets table: id INTEGER PRIMARY KEY AUTOINCREMENT, name TEXT NOT NULL UNIQUE, site TEXT NOT NULL DEFAULT 'factory-1', area TEXT NOT NULL DEFAULT '', line TEXT NOT NULL DEFAULT '', type TEXT NOT NULL DEFAULT 'machine', created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP
   - Indexes: idx_assets_site, idx_assets_name
   - asset_sensors table: id INTEGER PRIMARY KEY AUTOINCREMENT, asset_id INTEGER NOT NULL, sensor_id TEXT NOT NULL, sensor_type TEXT NOT NULL DEFAULT 'generic', topic TEXT NOT NULL DEFAULT '', created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (asset_id) REFERENCES assets(id) ON DELETE CASCADE, UNIQUE(asset_id, sensor_id)
   - Indexes: idx_asset_sensors_asset_id, idx_asset_sensors_sensor_id

2. Create `internal/storage/migrations/002_assets.down.sql`:
   - DROP TABLE IF EXISTS asset_sensors;
   - DROP TABLE IF EXISTS assets;

3. Extend `internal/storage/storage.go` with asset CRUD methods:

   a. Add types:
   - Asset struct: ID int64, Name, Site, Area, Line, Type string, CreatedAt, UpdatedAt time.Time
   - AssetSensor struct: ID, AssetID int64, SensorID, SensorType, Topic string, CreatedAt time.Time

   b. Add methods (using squirrel query builder, per existing pattern):
   - CreateAsset(ctx, *Asset) (*Asset, error) — INSERT RETURNING id, created_at, updated_at
   - GetAsset(ctx, id int64) (*Asset, error) — SELECT by id
   - ListAssets(ctx, page, limit int) ([]Asset, int, error) — SELECT with pagination, returns total count
   - CreateAssetSensor(ctx, assetID int64, sensor *AssetSensor) (*AssetSensor, error) — INSERT with FK
   - GetAssetSensors(ctx, assetID int64) ([]AssetSensor, error) — SELECT by asset_id

   c. Wrap all errors with context (per golang-error-handling skill): fmt.Errorf("creating asset: %w", err)

4. Verify migration applies cleanly by running existing tests (NewForTest doesn't use migrations, but New() does).
  </action>
  <verify>
    <automated>go test -race ./internal/storage/... -v</automated>
  </verify>
  <done>
    - Migration 002 files exist and create correct tables
    - Asset and AssetSensor types defined
    - CRUD methods work: CreateAsset, GetAsset, ListAssets, CreateAssetSensor, GetAssetSensors
    - UNIQUE constraints enforced (duplicate name → error)
    - CASCADE delete works (asset deletion removes sensors)
    - Pagination returns correct total count
    - go vet clean
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config → Plugin | MQTT config values consumed by plugin — must be validated |
| SQL queries → SQLite | Parameterized queries prevent SQL injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Tampering | SQL queries | high | mitigate | All queries use squirrel parameterized builder, no string concatenation |
| T-02-04 | Denial of Service | Config parsing | low | accept | YAML parsing is bounded by file size; v1 single-user |
| T-02-05 | Info Disclosure | Config defaults | low | accept | No secrets in config; MQTT port is local-only |
</threat_model>

<verification>
- `go test -race ./internal/config/...` passes
- `go test -race ./internal/storage/...` passes
- `go vet ./internal/config/... ./internal/storage/...` clean
- Existing Phase 1 tests still pass: `go test ./internal/... ./cmd/ml-elec/...`
- `go test ./...` full suite green
</verification>

<success_criteria>
- Config struct has MQTT, Validation, Assets sections with correct defaults
- Backward compatibility: missing YAML sections use defaults (per D-26)
- SQLite schema for assets and asset_sensors created by migration
- Asset CRUD methods work with pagination
- All tests pass, no regressions
</success_criteria>

<output>
Create `.planning/phases/02-plugin-sdk-mqtt-bridge/02-02-SUMMARY.md` when done
</output>
