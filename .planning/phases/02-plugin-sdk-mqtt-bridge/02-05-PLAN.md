---
phase: 02-plugin-sdk-mqtt-bridge
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-03, 02-04]
files_modified:
  - cmd/mock-plugin/main.go
  - cmd/mqtt-plugin/main_test.go
  - cmd/ml-elec/main.go
autonomous: true
requirements: [CORE-06, ACQ-01]
user_setup: []

must_haves:
  truths:
    - "Mock plugin updated to use new gRPC SDK (per D-07)"
    - "Crash isolation test: killing MQTT plugin does not crash core (SPEC acceptance)"
    - "MQTT plugin handles 100 msg/s with <100ms latency (SPEC acceptance, per D-17)"
    - "Existing Phase 1 tests still pass with new SDK"
    - "Core binary starts with MQTT plugin enabled"
  artifacts:
    - cmd/mock-plugin/main.go (updated)
    - cmd/mqtt-plugin/main_test.go (extended)
    - cmd/ml-elec/main.go (updated to launch MQTT plugin)
  key_links:
    - "Mock plugin → SDK validation (compile-time check)"
    - "Crash isolation → go-plugin child process management"
    - "Performance benchmark → 100 msg/s requirement"
---

<objective>
Validate the complete plugin ecosystem: update mock plugin to use new gRPC SDK (CORE-06), test crash isolation (ACQ-01), run performance benchmarks (D-17), and ensure the core binary integrates with the MQTT plugin.

Purpose: This is the integration validation plan — ensures all Phase 2 components work together and meet SPEC acceptance criteria.

Output: Updated mock plugin, crash isolation test, performance benchmark, and verified core+MQTT plugin integration.
</objective>

<execution_context>
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/workflows/execute-plan.md
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-CONTEXT.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-RESEARCH.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-SPEC.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-01-SUMMARY.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-03-SUMMARY.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-04-SUMMARY.md
@cmd/mock-plugin/main.go
@cmd/ml-elec/main.go
@internal/plugin/manager.go
@pkg/sdk/v1/plugin.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update mock plugin to use new gRPC SDK and integrate MQTT plugin launch</name>
  <files>cmd/mock-plugin/main.go, cmd/ml-elec/main.go</files>
  <read_first>
    - .agents/skills/golang-how-to/SKILL.md
    - .agents/skills/golang-error-handling/SKILL.md
    - .agents/skills/golang-naming/SKILL.md
    - .agents/skills/golang-grpc/SKILL.md
    - .agents/skills/golang-safety/SKILL.md
    - cmd/mock-plugin/main.go
    - cmd/ml-elec/main.go
    - internal/plugin/manager.go
    - pkg/sdk/v1/plugin.go
  </read_first>
  <action>
1. Update `cmd/mock-plugin/main.go` to use new gRPC SDK (per PATTERNS.md mock-plugin pattern):

   a. Implement both PluginLifecycle and SensorCollector interfaces:
   - Init(ctx, config): return nil (mock)
   - Start(ctx): return nil (mock)
   - Stop(ctx): return nil (mock)
   - Collect(ctx, *pb.CollectRequest): return &pb.CollectResponse{Accepted: true}, nil

   b. Update main() to use goplugin.Serve with GRPCPlugin:
   - Import sdk "ml-elec/pkg/sdk/v1"
   - Register "sensor" plugin as &sdk.GRPCPlugin{Impl: &MockPlugin{}, CollectImpl: &MockPlugin{}}
   - Use same HandshakeConfig from internal/plugin

   c. Remove old net/rpc implementation (SensorPluginRPC)

2. Update `cmd/ml-elec/main.go` to launch MQTT plugin:

   a. Add MQTT plugin launch in InitializeApp:
   - After pluginMgr creation, if "mqtt-plugin" is in enabledPlugins:
     - Build path to mqtt-plugin binary (look in ./bin/mqtt-plugin or configurable path)
     - Call pluginMgr.LaunchGRPC() with the MQTT plugin GRPCPlugin
   - Per D-03: broker is started by the plugin process itself (not by core)

   b. Add LaunchGRPC method to internal/plugin/manager.go:
   - Similar to existing Launch() but with AllowedProtocols: []plugin.Protocol{plugin.ProtocolGRPC}
   - Support both net/rpc (existing) and gRPC (new) plugins
   - Use VersionedPlugins if needed for backward compatibility (per RESEARCH Pitfall 2)

   c. Update shutdown to kill MQTT plugin before other plugins (LIFO order)

3. Verify: `go build ./cmd/ml-elec/... && go build ./cmd/mock-plugin/... && go build ./cmd/mqtt-plugin/...`
  </action>
  <verify>
    <automated>go build ./cmd/ml-elec/... && go build ./cmd/mock-plugin/... && go build ./cmd/mqtt-plugin/...</automated>
  </verify>
  <done>
    - Mock plugin implements gRPC SDK interfaces
    - Core binary builds with MQTT plugin launch support
    - All three binaries compile without error
    - go vet clean
  </done>
</task>

<task type="auto">
  <name>Task 2: Create crash isolation test and performance benchmark</name>
  <files>cmd/mqtt-plugin/main_test.go</files>
  <read_first>
    - .agents/skills/golang-how-to/SKILL.md
    - .agents/skills/golang-error-handling/SKILL.md
    - .agents/skills/golang-testing/SKILL.md
    - .agents/skills/golang-concurrency/SKILL.md
    - cmd/mqtt-plugin/main.go
    - cmd/mqtt-plugin/main_test.go
    - internal/plugin/manager.go
  </read_first>
  <action>
1. Add crash isolation test to `cmd/mqtt-plugin/main_test.go`:

   a. TestCrashIsolation (SPEC acceptance criterion):
   - Start core process (ml-elec binary)
   - Start MQTT plugin process as child
   - Verify core responds to /health
   - Kill MQTT plugin process (SIGKILL)
   - Wait 1 second
   - Verify core still responds to /health
   - Verify core logs plugin disconnection
   - Stop core process

   b. Implementation notes:
   - Use os/exec to launch binaries
   - Use process.Pid and syscall.Kill for crash simulation
   - Use HTTP client for health checks
   - Add timeouts to prevent test hanging
   - Use unique ports to avoid conflicts

2. Add performance benchmark (per D-17):

   a. TestBenchmarkMQTTToNATS:
   - Start broker
   - Connect MQTT client
   - Connect NATS client
   - Publish 100 messages sequentially
   - Measure time from MQTT publish to NATS receive
   - Assert average < 100ms per message
   - Use testing.B for proper benchmark

   b. TestBenchmarkConcurrentPublish:
   - Start broker
   - Connect 10 MQTT clients
   - Each publishes 10 messages (100 total)
   - Measure total throughput
   - Assert > 100 msg/s

3. Add MQTT plugin lifecycle test:

   a. TestMQTTPluginLifecycle:
   - Create plugin manager
   - Launch MQTT plugin via LaunchGRPC
   - Verify plugin is running (IsRunning returns true)
   - Kill plugin
   - Verify plugin is not running (IsRunning returns false)
   - ShutdownAll cleans up

4. Add integration test for full pipeline:

   a. TestFullPipelineMQTTToNATS:
   - Start embedded NATS server
   - Start MQTT broker
   - Connect MQTT client
   - Subscribe to NATS sensor.* subjects
   - Publish MQTT message (JSON format)
   - Verify NATS receives the message
   - Verify message content matches
  </action>
  <verify>
    <automated>go test -race ./cmd/mqtt-plugin/... -v -timeout 60s</automated>
  </verify>
  <done>
    - Crash isolation test passes: killing MQTT plugin does not crash core
    - Performance benchmark confirms <100ms latency and >100 msg/s throughput
    - Full pipeline test: MQTT → validate → NATS works end-to-end
    - MQTT plugin lifecycle (launch/kill/shutdown) works correctly
    - All tests pass with race detector
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Core → Plugin process | Child process isolation via go-plugin |
| MQTT plugin → Broker | In-process, trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-13 | Denial of Service | Plugin crash | high | mitigate | go-plugin child process isolation; core continues after plugin crash |
| T-02-14 | Elevation of Privilege | Plugin binary | medium | mitigate | Plugins run as child processes with no elevated privileges |
| T-02-15 | Tampering | Performance test | low | accept | Benchmark uses controlled environment; real-world may vary |
</threat_model>

<verification>
- `go test -race ./cmd/mqtt-plugin/... -v` passes (including crash isolation)
- `go test -race ./... -v` full suite green
- `go vet ./...` clean
- `make check-loc` still < 5000
- `make build` produces all three binaries
</verification>

<success_criteria>
- Mock plugin uses new gRPC SDK
- Crash isolation verified: core survives plugin crash
- Performance meets 100 msg/s and <100ms latency requirements
- Full pipeline (MQTT → NATS) works end-to-end
- All three binaries compile and integrate correctly
- No regressions in existing tests
</success_criteria>

<output>
Create `.planning/phases/02-plugin-sdk-mqtt-bridge/02-05-SUMMARY.md` when done
</output>
