---
phase: 02-plugin-sdk-mqtt-bridge
plan: 06
type: execute
wave: 4
depends_on: ["02-05"]
files_modified:
  - cmd/mqtt-plugin/main.go
  - cmd/mqtt-plugin/main_test.go
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
gap_closure: true
requirements:
  - ACQ-01
  - ACQ-02
  - ACQ-03
  - ACQ-04

must_haves:
  truths:
    - MQTT message handler validates payload, publishes to NATS, and persists to SQLite
    - QoS is configurable per topic via config.yaml (not hardcoded)
    - Exponential backoff configured on paho MQTT client reconnect
    - All validation/parsing errors are logged via slog, never silently ignored
  artifacts:
    - cmd/mqtt-plugin/main.go (updated message handler with NATS+storage+validation)
    - cmd/mqtt-plugin/main_test.go (tests for full pipeline, QoS, backoff, error logging)
    - internal/config/config.go (MQTTConfig with QoS and backoff fields)
    - internal/config/config_test.go (tests for new config fields)
  key_links:
    - MQTT message → ValidateMessage → NATS publish → storage.InsertSensor
    - MQTTConfig.QoS → SubscribeAndBridge subscription QoS
    - MQTTConfig.ReconnectBackoff → paho client backoff options
---

<objective>
Close all 6 verification gaps in the MQTT plugin: wire NATS publishing, integrate validation and storage in the message handler, make QoS configurable, add exponential backoff, and ensure all errors are logged.

Purpose: The MQTT plugin currently subscribes to topics but only logs messages — no data reaches NATS or SQLite. This plan completes the data pipeline so ESP32 sensor readings flow end-to-end: MQTT → validate → NATS → SQLite.

Output: Fully functional MQTT bridge plugin with validated, persisted, and published sensor data.
</objective>

<execution_context>
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/workflows/execute-plan.md
@/home/marwane/Documents/My-ML_Elec/.opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-VERIFICATION.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-CONTEXT.md
@.planning/phases/02-plugin-sdk-mqtt-bridge/02-SPEC.md
@cmd/mqtt-plugin/main.go
@cmd/mqtt-plugin/main_test.go
@internal/config/config.go
@internal/storage/storage.go
@internal/nats/nats.go

# Mandatory skills for executor
@.agents/skills/golang-how-to/SKILL.md
@.agents/skills/golang-error-handling/SKILL.md
@.agents/skills/golang-naming/SKILL.md
@.agents/skills/golang-testing/SKILL.md
@.agents/skills/golang-lint/SKILL.md
@.agents/skills/golang-grpc/SKILL.md
@.agents/skills/sqlite-database-expert/SKILL.md
@.agents/skills/mqtt-development/SKILL.md
@.agents/skills/validate-data/SKILL.md

## Struct Naming Clarification
The existing code has TWO structs that serve the same role:
- `App` (line 29) — legacy struct with Config, Broker, Client fields
- `MQTTPlugin` (line 301) — implements PluginLifecycle + SensorCollector interfaces

**Action:** Consolidate to `MQTTPlugin` as the single struct. Update `App` references in `main()` to use `MQTTPlugin`. Add `natsConn` and `store` fields to `MQTTPlugin`.

## NATS Architecture Clarification
The NATS server is embedded in the **core** process (`internal/nats/nats.go`), NOT in the MQTT plugin. The plugin is a child process that connects to the core's NATS server via the URL from config (`mqtt.nats_host:mqtt.nats_port`). Do NOT use `internal/nats.New()` in the plugin — that creates a new embedded server. Use `natsclient.Connect()` instead.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MQTTConfig with QoS, NATS, and backoff fields</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <behavior>
    - Test: MQTTConfig.QoS defaults to 1 when not set in YAML
    - Test: MQTTConfig.QoSPerTopic map is parsed from YAML (e.g. esp32/vibration: 0)
    - Test: MQTTConfig.NATSHost defaults to "127.0.0.1"
    - Test: MQTTConfig.NATSPort defaults to 4222
    - Test: MQTTConfig.ReconnectBackoff.InitialInterval defaults to "1s"
    - Test: MQTTConfig.ReconnectBackoff.MaxInterval defaults to "30s"
    - Test: MQTTConfig.ReconnectBackoff.Multiplier defaults to 2.0
    - Test: Full config YAML with new MQTT fields parses correctly
  </behavior>
  <action>
    Add fields to MQTTConfig struct in internal/config/config.go (per D-18, D-20):

    type MQTTConfig struct {
        Port         int               `yaml:"port"`
        ClientID     string            `yaml:"client_id"`
        CleanSession bool              `yaml:"clean_session"`
        KeepAlive    int               `yaml:"keep_alive"`
        QoS          byte              `yaml:"qos"`           // Default QoS for subscriptions (0 or 1)
        QoSPerTopic  map[string]byte   `yaml:"qos_per_topic"` // Per-topic QoS overrides
        NATSHost     string            `yaml:"nats_host"`     // NATS server host to connect to
        NATSPort     int               `yaml:"nats_port"`     // NATS server port to connect to
        ReconnectBackoff struct {
            InitialInterval string  `yaml:"initial_interval"` // e.g. "1s"
            MaxInterval     string  `yaml:"max_interval"`     // e.g. "30s"
            Multiplier      float64 `yaml:"multiplier"`       // e.g. 2.0
        } `yaml:"reconnect_backoff"`
        Topics struct {
            Subscribe string `yaml:"subscribe"`
            Status    string `yaml:"status"`
        } `yaml:"topics"`
    }

    Update DefaultConfig() to set:
    - QoS: 1
    - QoSPerTopic: empty map
    - NATSHost: "127.0.0.1"
    - NATSPort: 4222
    - ReconnectBackoff.InitialInterval: "1s"
    - ReconnectBackoff.MaxInterval: "30s"
    - ReconnectBackoff.Multiplier: 2.0

    Add table-driven tests in config_test.go covering defaults, YAML parsing, and per-topic QoS. Follow existing test patterns (table-driven, subtests).
  </action>
  <verify>
    <automated>cd /home/marwane/Documents/My-ML_Elec && go test ./internal/config/... -v -run TestMQTT 2>&1 | tail -20</automated>
  </verify>
  <done>MQTTConfig has QoS, QoSPerTopic, NATSHost, NATSPort, ReconnectBackoff fields with correct defaults; all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire NATS client and SQLite storage into MQTT plugin</name>
  <files>cmd/mqtt-plugin/main.go, cmd/mqtt-plugin/main_test.go</files>
  <behavior>
    - Test: ConnectToNATS returns a working nats.Conn when given a valid NATS server URL
    - Test: ConnectToNATS returns error when NATS server is unreachable
    - Test: MQTTPlugin.Start creates NATS connection and storage store
    - Test: MQTTPlugin.Stop closes NATS connection and storage cleanly
    - Test: Message handler publishes valid JSON to NATS sensor.{topic} subject
    - Test: Message handler calls storage.InsertSensor for each valid reading
    - Test: Message handler logs error and skips NATS/storage on validation failure
    - Test: Message handler logs error on JSON parse failure (never silently ignores)
  </behavior>
  <action>
    **Architecture note:** The NATS server is embedded in the **core** process (`internal/nats`), not in the MQTT plugin. The plugin is a child process that connects to the core's NATS server via the URL passed through config (`mqtt.nats_host:mqtt.nats_port`). The plugin does NOT use `internal/nats.New()` — that creates an embedded server. Instead, the plugin uses `natsclient.Connect()` to connect to the core's existing NATS server.

    1. Add natsclient import: natsclient "github.com/nats-io/nats.go"
    2. Add storage import: "ml-elec/internal/storage"

    3. Add ConnectToNATS function (connects to core's embedded NATS, not creating a new server):
    func ConnectToNATS(host string, port int, backoffCfg config.ReconnectBackoffConfig) (*natsclient.Conn, error) {
        url := fmt.Sprintf("nats://%s:%d", host, port)
        initialInterval, err := time.ParseDuration(backoffCfg.InitialInterval)
        if err != nil {
            initialInterval = 1 * time.Second
        }
        maxInterval, err := time.ParseDuration(backoffCfg.MaxInterval)
        if err != nil {
            maxInterval = 30 * time.Second
        }
        conn, err := natsclient.Connect(url,
            natsclient.MaxReconnects(-1),
            natsclient.ReconnectWait(initialInterval),
            natsclient.MaxReconnectInterval(maxInterval),
        )
        if err != nil {
            return nil, fmt.Errorf("connecting to NATS at %s: %w", url, err)
        }
        return conn, nil
    }

    4. Extend App struct to add NatsConn and Store fields (rename from App to MQTTPlugin for clarity — both names refer to the same struct):
    type MQTTPlugin struct {
        config  *config.Config
        broker  *mqtt.Server
        client  pahomqtt.Client
        natsConn *natsclient.Conn  // NATS connection to core's embedded server
        store    *storage.Store    // SQLite storage for persistence
    }
    Note: The existing code already has `MQTTPlugin` struct at line 301. Extend it with the two new fields. The `App` struct at line 29 is a legacy alias — update both or consolidate to `MQTTPlugin`.

    5. In Start() method, after connecting MQTT client:
    - Call ConnectToNATS(p.config.MQTT.NATSHost, p.config.MQTT.NATSPort, p.config.MQTT.ReconnectBackoff) and store in p.natsConn
    - Call storage.New(&p.config.Storage) and store in p.store
    - Log both connections at Info level

    6. In Stop() method, close NATS connection and storage before MQTT disconnect:
    - if p.natsConn != nil { p.natsConn.Close() }
    - if p.store != nil { p.store.Close() }

    7. Update main() function to do the same wiring (NATS + storage) after broker/client setup.

    8. Add tests using embedded NATS server (same pattern as TestBenchmarkMQTTToNATS) and storage.NewForTest for SQLite.
  </action>
  <verify>
    <automated>cd /home/marwane/Documents/My-ML_Elec && go test ./cmd/mqtt-plugin/... -v -run "TestConnectToNATS|TestMQTTPluginStart|TestMQTTPluginStop" 2>&1 | tail -20</automated>
  </verify>
  <done>NATS client connects to core's embedded NATS server; SQLite storage initialized; both cleaned up in Stop(); tests pass for connect/disconnect lifecycle</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement full message handler pipeline with QoS and backoff</name>
  <files>cmd/mqtt-plugin/main.go, cmd/mqtt-plugin/main_test.go</files>
  <behavior>
    - Test: Full pipeline — valid JSON MQTT message → validate → publish to NATS sensor.{topic} → insert into SQLite
    - Test: Invalid JSON → log error → no NATS publish → no SQLite insert
    - Test: Out-of-range value → log error → no NATS publish → no SQLite insert
    - Test: Future timestamp → log error → no NATS publish → no SQLite insert
    - Test: Empty payload → log error → no NATS publish → no SQLite insert
    - Test: Binary payload → validate → publish to NATS → insert into SQLite
    - Test: Per-topic QoS — topic esp32/vibration uses QoS 0, esp32/temperature uses QoS 1
    - Test: Exponential backoff — paho client options include configured backoff intervals
    - Test: Parse error always logged (slog.Error called), never silently dropped
  </behavior>
  <action>
    Replace the message handler in main() (lines 398-400) and in Start() with a full pipeline handler:

    handler := func(topic string, payload []byte) {
        // Level 3: Quality validation (payload size)
        if err := ValidateMessage(payload, buildValidationCfg(p.config.Validation)); err != nil {
            slog.Error("message validation failed", "topic", topic, "error", err, "payload_len", len(payload))
            return
        }

        // Parse JSON or binary payload
        reading, jsonErr := ParseJSONPayload(payload)
        if jsonErr != nil {
            // Try binary
            binResult, binErr := ParseBinaryPayload(payload)
            if binErr != nil {
                slog.Error("failed to parse payload", "topic", topic, "json_err", jsonErr, "bin_err", binErr)
                return
            }
            // Binary: publish raw to NATS
            natsSubject := "sensor." + topic
            if err := p.natsConn.Publish(natsSubject, payload); err != nil {
                slog.Error("NATS publish failed", "topic", topic, "subject", natsSubject, "error", err)
            }
            // Persist binary reading (one entry per sample count metadata)
            ts := time.Unix(0, binResult.Header.Timestamp)
            if err := p.store.InsertSensor(ctx, topic, float64(binResult.Header.SampleCount), ts); err != nil {
                slog.Error("storage insert failed", "topic", topic, "error", err)
            }
            return
        }

        // JSON: publish each sensor value to NATS
        natsSubject := "sensor." + topic
        if err := p.natsConn.Publish(natsSubject, payload); err != nil {
            slog.Error("NATS publish failed", "topic", topic, "subject", natsSubject, "error", err)
        }

        // Persist each sensor value to SQLite
        ts := time.Unix(reading.Timestamp, 0)
        for sensorName, value := range reading.Values {
            if err := p.store.InsertSensor(ctx, sensorName, value, ts); err != nil {
                slog.Error("storage insert failed", "sensor", sensorName, "error", err)
            }
        }
    }

    Add helper function buildValidationCfg that converts config.ValidationConfig to the local ValidationConfig type (parsing duration strings).

    Update SubscribeAndBridge to use configurable QoS:
    - Check p.config.MQTT.QoSPerTopic for the specific topic filter
    - Fall back to p.config.MQTT.QoS default
    - Pass the resolved QoS byte to client.Subscribe()

    func SubscribeAndBridgeWithQoS(client pahomqtt.Client, topicFilter string, qos byte, handler MessageHandler) error {
        token := client.Subscribe(topicFilter, qos, func(_ pahomqtt.Client, msg pahomqtt.Message) {
            handler(msg.Topic(), msg.Payload())
        })
        ...
    }

    Update ConnectClient to use backoff config:
    - Parse ReconnectBackoff.InitialInterval and MaxInterval via time.ParseDuration
    - Set SetInitialReconnectInterval and SetMaxReconnectInterval on paho options

    Add integration test TestFullPipelineMQTTToSQLite that:
    1. Starts embedded NATS server
    2. Creates storage.NewForTest SQLite DB
    3. Starts embedded MQTT broker
    4. Connects MQTT client + NATS client + storage
    5. Publishes valid JSON to MQTT
    6. Verifies NATS received the message on sensor.{topic}
    7. Verifies SQLite has the inserted reading

    Add negative tests for each validation failure path, verifying slog.Error is called (use a custom slog.Handler to capture log output).
  </action>
  <verify>
    <automated>cd /home/marwane/Documents/My-ML_Elec && go test ./cmd/mqtt-plugin/... -v -run "TestFullPipeline|TestMessageHandler|TestQoSConfig|TestBackoffConfig|TestErrorLogging" 2>&1 | tail -30</automated>
  </verify>
  <done>Message handler validates → publishes to NATS → persists to SQLite; QoS configurable per topic; backoff configured; all errors logged; all tests pass</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MQTT client → plugin | External ESP32 devices publish untrusted JSON/binary payloads |
| Plugin → NATS | Validated data crosses from plugin process to embedded NATS bus |
| Plugin → SQLite | Persisted data crosses from plugin process to shared database file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Tampering | MQTT message handler | high | mitigate | Three-level validation (quality, timestamp, range) rejects malformed/out-of-range data before NATS publish and SQLite insert |
| T-02-06-02 | Denial of Service | MQTT broker | medium | accept | Embedded broker handles QoS 0+1 only; QoS 2 disabled per D-18; rate limiting deferred to Phase 5 |
| T-02-06-03 | Information Disclosure | slog.Error logs | low | accept | Logs contain topic names and error messages; no PII stored per SPEC prohibition |
| T-02-06-04 | Elevation of Privilege | SQLite write | low | mitigate | Storage uses parameterized queries via squirrel; WAL mode with foreign keys enabled |
| T-02-06-05 | Tampering | NATS connection | medium | mitigate | Plugin connects to core's embedded NATS (localhost only); no external NATS exposure |
</threat_model>

<verification>
After all 3 tasks complete:

1. Run full test suite: `go test ./cmd/mqtt-plugin/... -v`
2. Run config tests: `go test ./internal/config/... -v`
3. Run all project tests: `go test ./...` (no regressions)
4. Verify MQTT plugin builds: `go build ./cmd/mqtt-plugin`
5. Verify the 6 gaps are closed:
    - NATS publishing: TestFullPipelineMQTTToNATS passes (message reaches core's NATS)
    - Validation integrated: TestMessageHandlerValidation fails on invalid data
    - Storage integrated: TestFullPipelineMQTTToSQLite has row in SQLite
    - QoS configurable: TestQoSPerTopic uses different QoS per topic
    - Backoff configured: ConnectClient and ConnectToNATS use configured backoff intervals
    - Errors logged: TestErrorLogging verifies slog.Error calls
</verification>

<success_criteria>
- All 6 VERIFICATION.md gaps closed (17/27 → 27/27 must-haves)
- Requirements ACQ-01, ACQ-02, ACQ-03, ACQ-04 satisfied
- All existing tests pass (no regressions)
- MQTT plugin handles 100 msg/s with <100ms latency (existing benchmark still passes)
- Crash isolation test still passes
</success_criteria>

<output>
Create `.planning/phases/02-plugin-sdk-mqtt-bridge/02-06-SUMMARY.md` when done
</output>
